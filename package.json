{
    "name": "yt-legacy-downloader",
    "version": "1.0.0",
    "description": "Media Downloader H.264",
    "main": "index.js",
    "scripts": {
        "dev": "npx concurrently \"node backend/server.js\" \"cd frontend && npx expo start --web\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
